package projetfinal

class tondeuses (var x: Int, var y: Int, dir: String ){
  //def avancer :
  //def deplacer(choice: Int): String = choice match
}

object Run extends App  {
  val tondeuse1 = new tondeuses (1, 2, "N")
  val tondeuse2 = new tondeuses (3, 3, "E")
}
